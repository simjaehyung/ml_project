# 검증 기록 (Validation Log)

> AI 제안을 검토·수정한 이력. 안내문 요구사항 최소 3건.
> 검증 사건 발생 시 즉시 기록합니다.

---

## #1. reservation_status_date — 누수 확정

- **날짜**: 2026-04-30
- **담당자**: 심재형
- **AI 제안**: reservation_status_date를 날짜 feature로 활용
- **검증 내용**: `02_leakage_check.py` L2 섹션 — 정상 체크인건은 체크아웃 시점, 취소건은 취소 처리 시점으로 기록됨. 어느 쪽이든 예약 시점에 알 수 없는 미래 정보.
- **결정**: DROP 확정
- **이유**: 타깃 정보가 직접 반영된 미래 시점값

---

## #2. 다지역 데이터 확보 제안 → 2개 호텔 한정·비교 축으로 재정의

- **날짜**: 2026-04-30
- **담당자**: 심재형
- **AI 제안**: 포르투갈 외 다지역 호텔 데이터를 추가 확보해 일반화 성능 향상
- **검증 내용**: 다지역 데이터 확보 시 호텔별 특성(운영 정책, 고객층, 지역 이벤트)이 희석됨. 학부생 프로젝트 범위에서 데이터 수집 현실성도 낮음.
- **결정**: 제안 기각. 2개 호텔(City/Resort) 비교 분석 축으로 활용.
- **이유**: 범위 축소가 분석 깊이를 오히려 높임. City 41.7% vs Resort 27.8% 취소율 차이 자체가 분석 포인트.

---

## #3. 가중치 주입 제안 → 단순 모델 + SHAP으로 데이터에 질문하기

- **날짜**: 2026-04-30
- **담당자**: 심재형
- **AI 제안**: 계절별 날씨 가중치를 feature engineering으로 주입
- **검증 내용**: 가중치 주입 시 "날씨가 중요하다"는 가설을 모델에 미리 넣는 것. 이는 우리의 연구 질문("날씨가 실제로 중요한가")과 순환 논리를 형성함.
- **결정**: 제안 폐기. 원본 변수 그대로 투입 후 SHAP으로 영향 측정.
- **이유**: 가설 주입 없이 데이터가 스스로 말하게 하는 것이 더 정직한 분석.

---

## #4. 날씨 메인 프레임 → 변수 중 하나로 격하

- **날짜**: 2026-04-30
- **담당자**: 심재형
- **AI 제안**: 프로젝트 타이틀을 "날씨 데이터 융합형 No-Show 예측 모델"로 설정
- **검증 내용**: 날씨가 메인이면 날씨 영향이 유의미해야만 프로젝트 성공. lead_time 평균 104일 환경에서 날씨를 메인으로 주장하는 것 자체가 시간 비대칭성 문제와 충돌.
- **결정**: 날씨를 외부 변수 중 하나로 격하. 메인 질문은 "어떤 변수가 취소에 영향을 주는가"로 재정의.
- **이유**: 어떤 결과가 나와도 valid한 분석이 되도록 프레임 설계. negative result도 허용.

---

## #5. 보류 컬럼 7개 처리 결정 — previous_cancellations 조건부 keep 외

- **날짜**: 2026-05-03
- **담당자**: 심재형
- **AI 제안**: 보류 컬럼별 drop/keep 데이터 검증 및 분류
- **검증 내용**:
  - `assigned_room_type`: 취소건 불일치율 낮음 → 체크인 시점 배정값 가설 지지 → DROP
  - `previous_bookings_not_canceled`: is_repeated_guest=1인데 값=0인 케이스 25.5% → 정합성 모순 → DROP
  - `days_in_waiting_list`: 타인 취소로 대기 해제 시 미래 정보 간접 반영 → DROP (발표 사례 활용)
  - `previous_cancellations`: 연도별 감소 추세 비직관적이나 논리적 모순 없음. 취소율 신호 강력(≥1이면 91%) → 조건부 KEEP
  - `agent`, `company`: 결측 자체가 예약 경로 신호 → 인디케이터 변환 후 KEEP
- **결정**: drop 3개, 조건부 keep 1개, 인디케이터 변환 keep 2개
- **이유**: 데이터 정합성과 예측 신호 강도를 동시에 고려. previous_cancellations는 Week 2 EDA에서 재검증.

---

## #6. 날씨 변수 다중공선성 — 3개 DROP 확정

- **날짜**: 2026-05-03
- **담당자**: 심재형 (김나리 제안 반영)
- **김나리 제안**: 날씨 변수 내 중복 측정값 정리 필요
  - `precipitation_sum` vs `rain_sum` — 포르투갈(온대기후)에서 사실상 동일 측정값
  - `temperature_2m_mean` — max·min에서 이미 충분히 표현됨
  - `wind_speed_10m_mean` — max가 더 직접적인 영향 지표
- **검증 내용**:
  - `rain_sum`: 포르투갈은 강수가 대부분 비(눈 거의 없음) → precipitation_sum과 상관관계 거의 1.0. 정보 중복.
  - `temperature_2m_mean` = (max + min) / 2 에 근사 → max·min으로 이미 내포됨
  - `wind_speed_10m_mean`: 취소 영향은 극단값(돌풍)이 더 직접적. mean은 max의 선형 함수에 가까움.
- **결정**: `rain_sum`, `temperature_2m_mean`, `wind_speed_10m_mean` DROP
- **이유**: 다중공선성 완화, feature 수 불필요한 증가 방지. 날씨 변수 10개 → 7개로 축소.

---

## #7. assigned_room_type — 단순 drop 대신 reserved_room_type만 사용

- **날짜**: 2026-05-03
- **담당자**: 이고은
- **AI 제안**: 누수 컬럼 단순 drop
- **검증 내용**: [Y1] 검증 결과 정상 체크인 건 18.78% / 취소 건 1.81% 불일치율 — 취소건은 reserved=assigned 디폴트, 정상건만 체크인 시점 갱신. 파생 변수 `room_changed = (reserved≠assigned)` 옵션은 정상건에서만 의미 있어 타깃 종속(가설 주입 순환 함정)으로 사전 배제.
- **결정**: `reserved_room_type` 만 모델 입력으로 사용 (예약 시점 확정값)
- **이유**: 단순 drop은 정보 손실. 같은 의미를 담은 시점 안전 컬럼이 존재하므로 그것을 대체로 사용.

---

## #8. previous_cancellations 정의 어긋남 발견 — Week 2 EDA 안건으로

- **날짜**: 2026-05-03
- **담당자**: 이고은
- **AI 제안**: 행 단위 시간 누수 검증을 위한 손님 ID 대리키 사용
- **검증 내용**: 손님 ID 대리키 자체가 추가 가정이라 가설 주입 함정 위험 — 시도 안 함. 대신 [Y3] 분포 분석에서 의외 발견: `is_repeated_guest=0`인데 `previous_cancellations≥1`인 행 약 2,674개 존재 → 두 컬럼 정의가 어긋나는 부분 확인.
- **결정**: AI 제안(대리키) 폐기 + 정의 어긋남 사실을 Week 2 EDA 재검증 안건으로 정리
- **이유**: 가설 주입 회피 원칙 일관성 유지 (검증기록 #3 흐름과 동일). 데이터 자체의 정합성 모순은 EDA로 풀고 결정.

---

## #9. deposit_type "Non Refund" — 누수 후보 격상, Phase 2 ablation 예약

- **날짜**: 2026-05-07
- **담당자**: 심재형
- **AI 제안**: deposit_type을 포함한 전처리 파이프라인 실행
- **검증 내용**: train 데이터에서 deposit_type=Non Refund 취소율 99.2% (10,461건 중 10,376건 취소). 시장 세그먼트 확인 결과 Groups 61%, Offline TA/TO 37% — 단체 예약 블록이 대부분. lead_time 평균 226일 (전체 평균 101일의 2.2배). 두 가지 가능성: (A) 단체 블록 운영 방식에서 발생하는 실제 패턴, (B) 취소 처리 이후 deposit_type이 "Non Refund"로 사후 업데이트되는 역방향 오염. 원본 데이터에 타임스탬프 없어 A/B 구분 불가.
- **결정**: MVP에서 `deposit_type` 포함 유지. Week 3 SHAP 결과에서 기여도 모니터링. Phase 2(Week 5)에서 deposit_type 제외 ablation 실험 필수.
- **이유**: 99.2% 취소율은 정상적인 예측 신호 범위를 벗어남. 모델이 이 컬럼에 과도하게 의존할 경우 SHAP 해석 왜곡. 단, 단체 예약 취소는 비즈니스 실제 현상이므로 사전 제거보다 데이터로 확인 후 결정.
